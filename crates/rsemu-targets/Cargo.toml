[package]
name = "rsemu-targets"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rsemu-core = { path = "../rsemu-core" }
rsemu-svd = { path = "../rsemu-svd" }

