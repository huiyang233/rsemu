[package]
name = "rsemu-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rsemu-core = { path = "../../crates/rsemu-core" }
rsemu-targets = { path = "../../crates/rsemu-targets" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "time"] }
minifb = "0.27"
