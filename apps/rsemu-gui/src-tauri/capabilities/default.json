{
  "$schema": "https://schemas.tauri.app/config/v2/capability.json",
  "identifier": "default",
  "description": "Default capability set for rsemu-gui",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
